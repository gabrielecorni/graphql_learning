# Simple Server Author-Books

Retrieve books and authors details without persisting changes to a database (in-memory changes).

To run:

    (create/activate virtualenv)
    python3 -m pip install -r requirements.txt
    python3 main.py
